<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".QRCodeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="35">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 快捷方式 -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".CameraScanActivity"
            android:label="Camera Scan" />

        <activity
            android:name=".ScanImageActivity"
            android:label="@string/scan_image" />

        <activity
            android:name=".GenerateActivity"
            android:label="@string/generate_qr" />

        <activity
            android:name=".ResultActivity"
            android:label="@string/scan_results" />

        <activity
            android:name=".VideoScanActivity"
            android:label="Video Scan" />

        <activity
            android:name=".BatchGenerateActivity"
            android:label="@string/batch_generate" />

        <activity
            android:name=".BatchResultActivity"
            android:label="@string/batch_result" />

        <!-- 连续扫描模式 -->
        <activity
            android:name=".ContinuousScanActivity"
            android:label="@string/continuous_scan" />

        <activity
            android:name=".PrivacySettingsActivity"
            android:label="@string/privacy_settings" />

        <activity
            android:name=".DatabaseSecurityActivity"
            android:label="@string/database_security" />

        <activity
            android:name=".BackupActivity"
            android:label="@string/backup_restore" />

        <!-- 桌面小组件 -->
        <receiver
            android:name=".QuickScanWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_scan_info" />
        </receiver>

        <receiver
            android:name=".QuickGenerateWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_quick_generate_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
